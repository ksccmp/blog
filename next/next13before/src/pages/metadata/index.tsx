import MetadataLayout from '../../layouts/metadata/layout';

const Metadata = () => {
    return (
        <MetadataLayout>
            <div>
                <h2>Metadata Page</h2>
            </div>
        </MetadataLayout>
    );
};

export default Metadata;
